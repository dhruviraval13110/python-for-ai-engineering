# Optimization

Optimization asks: which parameters minimize an objective?

A practical loop is **initialize → predict → compute loss → gradient → update → evaluate**.

Compare batch gradient descent, stochastic updates, learning rates and regularization. Always plot the loss curve and inspect whether optimization is stable.
