# Probability for ML

Key ideas: random variables, conditional probability, Bayes rule, expectation, variance, independence, likelihood.

Bayes rule: $P(A|B)=P(B|A)P(A)/P(B)$.

### ML connection
Naive Bayes, probabilistic classification, Bayesian inference, uncertainty estimation and generative modeling all depend on probability.
