# Datasets

Do not commit private, licensed or unnecessarily large datasets. Prefer documented download instructions and deterministic preprocessing scripts.

For every dataset record: source, license, access date, schema, target definition, row/feature counts when measured, missingness, split strategy and known limitations.
