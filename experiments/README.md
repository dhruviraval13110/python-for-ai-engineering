# Experiments

This directory is the laboratory notebook layer. Every experiment should be reproducible and should distinguish **observed evidence** from interpretation.

## Required structure

`hypothesis → baseline → controlled change → results → error analysis → conclusion → next experiment`

Do not optimize against the test set. Do not report cherry-picked runs. Store configuration, split strategy and metric definitions with results.
